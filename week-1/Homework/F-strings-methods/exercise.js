// Start by creating a variable `message`
 var name = "name";
var nameLength = name.length;
 
 function createGreeting(name, namelength){

return `My name is ${name} and my name is ${nameLength} characters long`
 }

console.log(createGreeting('Isha',4));
